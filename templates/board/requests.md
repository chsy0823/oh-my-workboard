# Request Queue

> Cross-member requests (review / work / decision / handoff).
> Format: `{requester} → {receiver}: [{project}] {content} — {YYYY-MM-DD}`
> No-duplicate rule: a queued request must NOT also appear as `@today` on the receiver's workboard.
> Exception — Handoffs: a handoff is a substantial new task for the receiver. On pickup, the receiver mirrors it as a workboard top-level leaf AND removes it from this queue.
> Optional sub-lines: `📎 artifact`, `🔍 focus`, `⏰ YYYY-MM-DD` deadline, `🔄 changes-requested → @{requester} in-progress (MM-DD)`, `🔄 awaiting-rereview (MM-DD)`

## PR Reviews
-

## Design Reviews
-

## Work Requests
-

## Decisions Needed
-

## Handoffs
-
